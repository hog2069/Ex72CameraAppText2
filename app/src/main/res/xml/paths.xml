<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!--내부 저장소 File 경로-->
    <files-path
        name="files"
        path="."/>

    <!--외부 저장소 중에서 공용영역 경로-->
    <external-path
        name="external"
        path="."/>

    <!--외부 저장소 중에서 앱 전용영역 경로-->
    <external-files-path
        name="external_file"
        path="."/>

</paths>